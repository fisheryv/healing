---
name: 未实现功能清单
overview: 对照 PRD（docs/希音_Healing_App-产品需求文档.md）全面审查 src/src 下代码，列出所有尚未实现或仅部分实现的功能，按模块分类呈现，供用户决定后续优先实现哪些功能。
todos:
  - id: audit-account
    content: 审查用户账户体系未实现功能（手机号Tab、第三方登录、锁定、昵称页、密码规则、验证码有效期、旧密码校验、自动登录）
    status: pending
  - id: audit-home-library
    content: 审查首页和曲库未实现功能（推荐更多跳转、收藏Tab定位、滑动删除、30秒试听限制）
    status: pending
  - id: audit-focus-profile
    content: 审查专注模式和个人中心未实现功能（褪色恢复动画、震动反馈、画廊作品数统计、账号脱敏、注销确认输入）
    status: pending
  - id: audit-misc
    content: 审查其他未实现功能（404页面、登录守卫、反馈邮件客户端、未登录限制）
    status: pending
---

## 用户需求
对照 PRD 文档（docs/希音_Healing_App-产品需求文档.md），全面检查现有代码实现，找出所有未实现或部分实现的功能，分类报告。

## 产品概述
希音（Healing）是一款面向 ADHD 群体的专注辅助 App，通过专注音乐 + 水墨（Suminagashi）渲染引擎实时生成抽象艺术，将专注过程具象化。包含用户账户、首页、专注模式、曲库、调音台、画廊、个人中心七大模块。当前为 Web 前端 Demo（React + Vite），使用 localStorage 模拟后端。

## 核心功能（未实现/部分实现清单）
以下为对照 PRD 逐模块检查后的结果：
- 用户账户：手机号注册/登录、第三方登录、登录锁定、昵称设置页、密码规则
- 首页：推荐"更多"跳转目标、收藏"更多"定位Tab
- 曲库：外部参数切换Tab
- 专注模式：防分心机制下半透明恢复逻辑
- 画廊/作品详情：分享功能已实现
- 个人中心：统计指标缺失一项
- 设置：修改密码不校验旧密码
- 验证码有效期不符合PRD
- 404兜底页缺失


## 技术现状
- **前端框架**: React 18 + Vite
- **路由**: react-router-dom v6
- **状态管理**: React Context + localStorage 持久化（store.jsx）
- **音频引擎**: Web Audio API（audioEngine.js），三轨混音 + 实时频谱分析
- **渲染引擎**: Canvas 2D + Suminagashi 水墨流体模拟（suminagashi.js）
- **图标**: lucide-react
- **国际化**: 自建 i18n.js（en/zh 双语）
- **数据**: 静态本地数据（data.js），无后端 API

## 未实现功能详细分析

### 一、用户账户体系（PRD 第2章）

#### 1.1 手机号注册/登录 — 完全缺失
- **PRD 要求**: 注册/登录页顶部有"手机号"和"邮箱"两个 Tab，默认选中手机号。支持手机号+验证码注册和登录。
- **实际代码**: `Login.jsx` 和 `SignUp.jsx` 仅实现邮箱注册/登录，无 Tab 切换、无手机号输入、无国家区号选择器、无验证码登录模式切换。
- **涉及文件**: `src/src/pages/Login.jsx`、`src/src/pages/SignUp.jsx`、`src/src/store.jsx`

#### 1.2 第三方登录 — 完全缺失
- **PRD 要求**: 登录页底部用分割线隔开，排列 Google、Apple ID、微信三个登录按钮。
- **实际代码**: `Login.jsx` 无任何第三方登录 UI 和逻辑。
- **涉及文件**: `src/src/pages/Login.jsx`

#### 1.3 登录安全限制（5次错误锁定30分钟）— 缺失
- **PRD 要求**: 密码连续错误 5 次，账号临时锁定 30 分钟，显示"账号已锁定，请 X 分钟后再试"。
- **实际代码**: `Login.jsx` 的 `handleLogin`（第28-43行）无错误次数计数和锁定逻辑。`store.jsx` 的 `auth.login` 也无锁定机制。
- **涉及文件**: `src/src/pages/Login.jsx`、`src/src/store.jsx`

#### 1.4 首次登录昵称设置页 — 缺失
- **PRD 要求**: 注册或首次第三方登录成功后，展示昵称设置页（头像占位圆、昵称输入框、完成按钮、跳过文字链）。
- **实际代码**: `App.jsx` 路由中无昵称设置页路由。`SignUp.jsx` 第78-79行注册成功后直接 `nav('/home')`。
- **涉及文件**: `src/src/App.jsx`、`src/src/pages/SignUp.jsx`

#### 1.5 密码规则不完整
- **PRD 要求**: 密码 8-20 位，需同时包含字母和数字。
- **实际代码**: `SignUp.jsx` 第60-61行仅校验 `password.length < 6`，无 8-20 位限制，无字母+数字组合校验。`Login.jsx` 第23行同。`SettingsPage.jsx` 第46行修改密码同样仅校验 `length < 6`。
- **涉及文件**: `src/src/pages/SignUp.jsx`、`src/src/pages/Login.jsx`、`src/src/pages/SettingsPage.jsx`、`src/src/pages/ForgotPassword.jsx`

#### 1.6 验证码有效期不符合PRD
- **PRD 要求**: 邮箱验证码有效期 10 分钟。
- **实际代码**: `SignUp.jsx` 第44行和 `ForgotPassword.jsx` 第45行均设倒计时 60 秒，验证码实际无过期机制。
- **涉及文件**: `src/src/pages/SignUp.jsx`、`src/src/pages/ForgotPassword.jsx`

#### 1.7 修改密码不校验旧密码
- **PRD 要求**: 修改密码需先验证当前密码。
- **实际代码**: `SettingsPage.jsx` 第37-58行 `handleChangePassword` 有旧密码输入框（第117-131行），但实际未校验旧密码——`auth.changePassword` 直接设置新密码。`store.jsx` 第150行注释也写明"不校验旧密码"。
- **涉及文件**: `src/src/pages/SettingsPage.jsx`、`src/src/store.jsx`

#### 1.8 忘记密码流程未自动登录
- **PRD 要求**: 密码重置后自动登录进入首页。
- **实际代码**: `ForgotPassword.jsx` 第77行 `setDone(true)` 后显示成功页，仅提供"返回登录"按钮，未自动登录。
- **涉及文件**: `src/src/pages/ForgotPassword.jsx`

### 二、首页（PRD 第3章）

#### 2.1 推荐"更多"跳转目标错误
- **PRD 要求**: 点击"更多"跳转至**全部推荐列表页**（竖向列表展示全部推荐歌曲）。
- **实际代码**: `Home.jsx` 第52行跳转到 `/library`（曲库页），且项目中无推荐列表页路由和组件。
- **涉及文件**: `src/src/pages/Home.jsx`、`src/src/App.jsx`

#### 2.2 "我的收藏"更多未定位Tab
- **PRD 要求**: 点击"更多"跳转至曲库页并定位到"我的收藏"Tab。
- **实际代码**: `Home.jsx` 第69行跳转 `nav('/library')` 未传 `state: { tab: 'fav' }`。`Library.jsx` 第14行 `tab` 默认 `'official'`，不接收 `location.state`。
- **涉及文件**: `src/src/pages/Home.jsx`、`src/src/pages/Library.jsx`

### 三、曲库（PRD 第5章）

#### 3.1 Tab 切换不支持外部参数
- **PRD 要求**: 从首页"我的收藏"更多跳转时自动定位到收藏Tab。
- **实际代码**: `Library.jsx` 第14行 `const [tab, setTab] = useState('official')`，不读取 `useLocation().state`。
- **涉及文件**: `src/src/pages/Library.jsx`

#### 3.2 "我创建的"Tab 删除用 × 按钮而非滑动删除
- **PRD 要求**: 向左滑动条目出现红色【删除】按钮。
- **实际代码**: `Library.jsx` 第98-101行使用 `×` 图标点击删除，非滑动删除。
- **涉及文件**: `src/src/pages/Library.jsx`

#### 3.3 "我的收藏"Tab 缺少滑动取消收藏
- **PRD 要求**: 向左滑动条目出现【取消收藏】按钮。
- **实际代码**: `Library.jsx` 第140-143行使用心形图标点击切换，非滑动删除。
- **涉及文件**: `src/src/pages/Library.jsx`

#### 3.4 歌曲详情页未实现"30秒试听"限制
- **PRD 要求**: 试听播放 30 秒预览片段，循环播放。
- **实际代码**: `Player.jsx` 播放完整歌曲，无 30 秒截断逻辑。
- **涉及文件**: `src/src/pages/Player.jsx`

### 四、专注模式（PRD 第4章）

#### 4.1 防分心恢复后褪色未真正反向动画恢复
- **PRD 要求**: 15 秒内放回，褪色动画反向恢复，线条回到原清晰度。
- **实际代码**: `FocusSession.jsx` 第137-146行恢复时直接 `setDistracted(false)`，CSS class 切换但无真正的反向褪色动画（无 transition 逐步恢复），半透明是瞬切。
- **涉及文件**: `src/src/pages/FocusSession.jsx`、`src/src/styles.css`

#### 4.2 专注完成缺少震动反馈
- **PRD 要求**: 设备产生短促双震动提示 + 轻柔完成提示音。
- **实际代码**: `FocusSession.jsx` 第151-168行 `finishSession` 无 `navigator.vibrate()` 调用，无完成提示音。
- **涉及文件**: `src/src/pages/FocusSession.jsx`

#### 4.3 专注完成缺少呼吸光晕动画
- **PRD 要求**: 画布出现约 2 秒的呼吸光晕动画（柔光向外扩散再收缩）。
- **实际代码**: `FocusSession.jsx` 第459-463行奖励阶段有 `reward-halo` div，但需确认 CSS 是否有完整 2 秒扩散收缩动画。
- **涉及文件**: `src/src/pages/FocusSession.jsx`、`src/src/styles.css`

### 五、个人中心（PRD 第8章）

#### 5.1 统计指标缺少"画廊作品数"
- **PRD 要求**: 5 项统计指标，包括画廊作品数。
- **实际代码**: `Profile.jsx` 第417-422行仅展示 4 项（累计专注时长、累计完成次数、当前连续天数、最长连续天数），缺少"画廊作品数"。
- **涉及文件**: `src/src/pages/Profile.jsx`

#### 5.2 账号信息未脱敏显示
- **PRD 要求**: 账号信息以星号脱敏显示（如 138****8888）。
- **实际代码**: `Profile.jsx` 第335行直接显示 `user?.account`，未脱敏。
- **涉及文件**: `src/src/pages/Profile.jsx`

#### 5.3 注销账号未要求手动输入"确认注销"
- **PRD 要求**: 需用户手动输入"确认注销"后方可执行。
- **实际代码**: `SettingsPage.jsx` 第213-229行仅有按钮确认弹框，无文本输入验证。
- **涉及文件**: `src/src/pages/SettingsPage.jsx`

### 六、其他

#### 6.1 404 兜底页面缺失
- **PRD 要求**: 隐含需求，路由匹配失败应有兜底页。
- **实际代码**: `App.jsx` 第54行 `<Route path="*" element={<Navigate to="/home" replace />} />` 直接重定向到首页，无 404 页面。
- **涉及文件**: `src/src/App.jsx`

#### 6.2 未登录用户功能限制未实现
- **PRD 要求**: 未登录用户访问专注/调音台/画廊/我的页面时，弹出提示框或跳转登录页。
- **实际代码**: `App.jsx` 路由未做任何登录守卫（Route Guard），未登录用户可直接访问所有页面。
- **涉及文件**: `src/src/App.jsx`

#### 6.3 意见反馈未打开邮件客户端
- **PRD 要求**: 意见反馈跳转反馈表单页或打开系统邮件客户端（预填收件地址）。
- **实际代码**: `SettingsPage.jsx` 第351-370行 `AboutPage` 的 feedback 类型提供文本框输入，提交后仅 Toast 提示，无邮件客户端调用。
- **涉及文件**: `src/src/pages/SettingsPage.jsx`


## Agent Extensions
### SubAgent
- **code-explorer**
  - Purpose: 深度探索代码库，验证未实现功能的准确性，查找遗漏的未实现项
  - Expected outcome: 产出完整的未实现功能清单，包含文件路径和行号
